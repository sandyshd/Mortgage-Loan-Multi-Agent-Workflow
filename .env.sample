# Azure AI Foundry project endpoint (from portal → Project → Overview → Endpoint)
PROJECT_ENDPOINT=https://<foundry-name>.services.ai.azure.com/api/projects/<project-name>

# Model deployment name (deploy in Foundry portal → Deployments)
MODEL_DEPLOYMENT=gpt-4o
